// Yacht Details Page (dynamic route)
// Add your code here

export default function YachtPage({ params }: { params: { id: string } }) {
  return (
    <div>
      {/* Yacht details for ID: {params.id} */}
    </div>
  )
}
